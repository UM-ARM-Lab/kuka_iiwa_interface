package victor_lcm_interface;

struct cartesian_impedance_parameters
{
    victor_lcm_interface.cartesian_value_quantity cartesian_stiffness;
    double nullspace_stiffness;
    victor_lcm_interface.cartesian_value_quantity cartesian_damping;
    double nullspace_damping;
}
