package victor_lcm_interface;

struct cartesian_path_execution_parameters
{
    victor_lcm_interface.cartesian_value_quantity max_velocity;
    victor_lcm_interface.cartesian_value_quantity max_acceleration;
    double max_nullspace_velocity;
    double max_nullspace_acceleration;
}
