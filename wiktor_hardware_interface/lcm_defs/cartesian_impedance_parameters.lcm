package wiktor_hardware_interface;

struct cartesian_impedance_parameters
{
    wiktor_hardware_interface.cartesian_value_quantity cartesian_stiffness;
    double nullspace_stiffness;
    wiktor_hardware_interface.cartesian_value_quantity cartesian_damping;
    double nullspace_damping;
}
